import styled from "styled-components";

export const EntireWrapper = styled.div`
  .header {
    height: 80px;
  }
  .filter {
    position: relative;
    height: 48px;
    padding-left: 16px;
  }
`;
